const lista = document.querySelectorAll("h2"); //Wyszukanie wszystkich elementów ze znacznikiem h2
console.log(lista [0].innerHTML);
for(const element of lista){ //Wypisanie każdego znalezionego elementu ze znacznikiem h2
    console.log(element);
}